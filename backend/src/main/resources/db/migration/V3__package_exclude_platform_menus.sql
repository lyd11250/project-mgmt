-- =====================================================================
-- V3: 平台专属菜单不进可订阅套餐（第 1 期-A2 修订）
-- 设计：菜单/套餐/租户管理为平台专属，只通过超管通配 * 可见，
--       绝不放入任何可被租户订阅的套餐。V2 的 FULL 套餐误含这些菜单，此处剔除。
-- 平台租户的超管靠通配 * 看全部，不依赖套餐内容，故剔除无副作用。
-- =====================================================================

-- 从 FULL 套餐（id=2）移除菜单管理(13)/套餐管理(14)/租户管理(15)及其按钮子项
DELETE FROM sys_package_menu
WHERE package_id = 2
  AND menu_id IN (13, 131, 132, 133, 14, 141, 142, 143, 144, 15, 151);
