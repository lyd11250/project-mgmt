-- =====================================================================
-- V6: 移除 sys_user.person_id（G13：personId 语义悬空，基座无人员域）
-- 基座不内置人员域，该列暗示的"人员"实体不存在，移除以免误导；
-- 如上层 biz/* 模块需要人员绑定，应在各自模块的表中维护关联。
-- =====================================================================

ALTER TABLE sys_user DROP COLUMN person_id;
COMMENT ON TABLE sys_user IS '系统用户';
